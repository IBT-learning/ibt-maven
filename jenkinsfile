pipeline {
      agent any

stages{
        stage('Hello') {
        steps {
         echo "hello World"
          }
     }
     stage(‘hi’) {
      steps{
            echo "hi"
     }
   }
    stage(‘howdy’) {
         steps{
               echo "howdy"
        }
      }
      stage('git checkout') {
      when {
      expression {
                    env.BRANCH_NAME=='main'
        }
      }
      steps{
      git branch: 'feature_malika', changelog: false, credentialsId: 'git_password', poll: false, url: 'https://github.com/IBT-learning/ibt-maven.git'
    }
   }
     stage('list files') {
     steps{
           sh 'ls -lrt' //mac-unix
          }
        }
     }
}