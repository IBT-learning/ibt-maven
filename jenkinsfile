pipeline {
    agent any
    parameters {
            string(name: 'Branch_Name', defaultValue: 'main', description: 'Input the Branch name to Build')
            choice(name: 'Choice', choices: ['One', 'Two', 'Three'], description: 'Make a selection')
    }
    stages {
        stage('Test') {
            steps {
                echo 'Hello World'
            }
        }
	    stage('Validate') {
            steps {
                echo 'Hello World, this is my second project'
            }
        }
        stage('Compile') {
            steps {
                 echo 'Hello World, this is my third line of project'
            }
        }
        stage('Package') {
            steps {
                  git branch: '$Branch_Name', changelog: false, credentialsId: 'jenkin_maven-pumej', poll: false, url: 'https://github.com/IBT-learning/ibt-maven.git'
            }
        }
        stage('Deploy') {
            steps {
                  // bat 'dir'
                   sh 'ls -ltr'
            }
        }
    }
}