pipeline{
    agent any

    stages{
        stage('Git checkout'){
            steps{
                git branch: 'josephine2', changelog: false, credentialsId: 'ibt', poll: false, url: 'https://github.com/IBT-learning/ibt-maven.git'
            }

        }

    }
}