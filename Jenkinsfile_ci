pipeline {
    agent any
    tools {
    maven 'maven-3.8.6' 
    }
    stages {
        stage('Hello') {
            steps {
                echo 'Hello Github'
                checkout changelog: false, poll: false, scm: scmGit(branches: [[name: '*/feature_koby2']], extensions: [], userRemoteConfigs: [[credentialsId: 'k-github', url: 'https://github.com/IBT-learning/ibt-maven.git']])
            }
        }
        stage('validate') {
            steps {
                sh "mvn validate"
            }
        }
    }
}
