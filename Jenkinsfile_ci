pipeline {
    agent any
    parameters {
        string(name: 'Branch', defaultValue:'feature_koby2', description: 'enter branch to build')
    }
    stages {
        stage('Git Checkout') {
            steps {
            git branch: '${Branch}', changelog: false, credentialsId: 'for-github', poll: false, url: 'https://github.com/IBT-learning/ibt-maven.git'            }
        }
        stage('Build') {
            steps {
                echo 'Hello World'
            }
        }
        stage('Test') {
            steps {
                echo 'Test'
            }
        }
        stage('Deploy') {
            steps {
            echo 'Deploy'
            }
        }
    }
}