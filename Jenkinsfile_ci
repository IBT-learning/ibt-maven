pipeline{
  agent any

  stages{
    stage('git checkout'){
      steps{
      git branch: 'feature_benny', credentialsId: 'Github_crnd_benny', url: 'https://github.com/IBT-learning/ibt-maven.git'
         }
       } //stage1

    stage('validating'){
          steps{
              withMaven(maven: 'Maven_3.8') {
                 sh 'mvn validate'
               }
             }
           } //stage2

  } //stages
} //pipeline