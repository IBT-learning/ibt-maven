piprline {
    agent anystages{
        stage('Git Checkout'){
            steps{
                echo "git checkout"
                git branch: 'feature-Martin2', changelog: false, credentialsId: 'mnchuks', poll: false, url: 'https://github.com/IBT-learning/ibt-maven.git'
            }
        }
        stage ('Validate'){
            steps{
                
            }
        }
    }
}