@Library('jenkins-library')_
pipeline{
    agent any

    stages {
    stage('Git checkout') {
                steps {
                    checkout changelog: false, poll: false, scm: [$class: 'GitSCM', branches: [[name: '*/feature_oakisanya']], extensions: [], userRemoteConfigs: [[credentialsId: 'GITHUB_OAKISANYA', url: 'https://github.com/IBT-learning/ibt-maven.git']]]
                }
            }
     stage('Validate')
     {
        steps{
            sh "mvn validate"
        }
     }
     stage('Compile')
          {
             steps{
                 sh "mvn compile"
             }
          }
     stage('SonarScan')
     {
        steps{
               sonarScan()
        }
     }
 }
}