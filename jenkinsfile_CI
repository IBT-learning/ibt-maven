pipeline {
agent any

stages {
    stage ('Git checkout'){
        steps {
            checkout scmGit(branches: [[name: '*/feature_lili']], extensions: [], userRemoteConfigs: [[url: 'https://github.com/IBT-learning/ibt-maven.git']])
    }
}

}