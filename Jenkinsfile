pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
            }
        }
       stage('trying jenkinsfile') {
                   steps {
                       echo 'This is running from github'
                   }
               }
    }
}