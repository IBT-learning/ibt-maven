pipeline {
    agent any
    environment {
      PATH = "/usr/local/bin:${env.PATH}"
    }
    parameters{
    string(name:'Branch_Name', defaultValue:'main', description:'Enter the branch to build')
    choice(name: 'CHOICES', choices: ['one', 'two', 'three'], description: 'choose a number')
    }
    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
            }
        }
        stage('Hi') {
            steps {
                echo 'Hi'
            }
        }
         stage('test') {
            steps {
                echo 'test'
            }
        }
          stage('pipeline') {
               steps {
                   echo 'test2'
            }
        }
          stage('Git checkout') {
                steps{
                    checkout changelog: false, poll: false, scm: scmGit(branches: [[name: '*/feature_mike1']], extensions: [], userRemoteConfigs: [[credentialsId: 'git_cred_omalives', url: 'https://github.com/IBT-learning/ibt-maven.git']])
                     sh 'dir'
            }
          }
    }
}
