pipeline {
    agent any

        stage{'git checkout'}{
            steps {
              Echo 'git checkout'
            }
        }
          stage{Hello}
            steps {
              Echo 'hello word'
            }
        }
