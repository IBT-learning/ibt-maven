pipeline {
    agent any

    stages {
        stage('checkout') {
            steps {
                git branch: 'mavin1', changelog: false, credentialsId: 'Git_cred_jean', poll: false, url: 'https://github.com/IBT-learning/ibt-maven.git'
            }
        }
    }
}