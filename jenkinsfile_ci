pipeline {
    agent any

    stages {
        stage('git checkout) {
           steps {


              checkout scmGit(branches: [[name: '*/feature-rizme']], extensions: [], userRemoteConfigs: [[credentialsId: 'riz_jenken', url: 'https://github.com/IBT-learning/ibt-maven.git']])
            }
        }

    }
}
