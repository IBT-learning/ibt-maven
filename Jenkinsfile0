pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
            }
        }
        stage('Hi') {
            steps {
                echo 'Hi Mike'
            }
        }
        stage('Hiya') {
            steps {
                echo 'Hi Chichi Man'
            }
        }
        stage('testing jenkins') {
             steps {
                 echo 'jenkins'
             }
         }
        stage('Git checkout'){
            steps{
                checkout changelog: false, poll: false, scm: scmGit(branches: [[name: '*/feature_mike1']], extensions: [], userRemoteConfigs: [[credentialsId: 'mike_db_connection', url: 'https://github.com/IBT-learning/ibt-maven.git']])
            }
        }
    }
}
